package MSG.traditional;

import MSG.MSGConstraints;

public class AORSMetaMutant {

	public static byte AORS(byte originalFinal, int op, String mutantID
	/* , boolean isForStmts */) {

		byte originalIntermediate = originalFinal;
		if (MSGConstraints.U_PRE_INCREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			originalIntermediate = (byte) (originalFinal - 1);
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			originalIntermediate = (byte) (originalFinal + 1);
		}

		return originalIntermediate;
	}

	public static char AORS(char originalFinal, int op, String mutantID
	/* boolean isForStmts */) {

		char originalIntermediate = originalFinal;
		if (MSGConstraints.U_PRE_INCREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			originalIntermediate = (char) (originalFinal - 1);
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			originalIntermediate = (char) (originalFinal + 1);
		}

		return originalIntermediate;
	}

	public static double AORS(double originalFinal, int op, String mutantID
	/* , boolean isForStmts */) {
		double originalIntermediate = originalFinal;
		if (MSGConstraints.U_PRE_INCREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			originalIntermediate = originalFinal - 1;
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			originalIntermediate = originalFinal + 1;
		}

		return originalIntermediate;
	}

	public static float AORS(float originalFinal, int op, String mutantID
	/* , boolean isForStmts */) {
		float originalIntermediate = originalFinal;
		if (MSGConstraints.U_PRE_INCREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			originalIntermediate = originalFinal - 1;
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			originalIntermediate = originalFinal + 1;
		}

		return originalIntermediate;
	}

	public static int AORS(int originalFinal, int op, String mutantID
	/* , boolean isForStmts */) {

		int originalIntermediate = originalFinal;

		if (MSGConstraints.U_PRE_INCREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			originalIntermediate = (byte) (originalFinal - 1);
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			originalIntermediate = (byte) (originalFinal + 1);
		}

		return originalIntermediate;
	}

	public static long AORS(long originalFinal, int op, String mutantID
	/* , boolean isForStmts */) {
		long originalIntermediate = originalFinal;
		if (MSGConstraints.U_PRE_INCREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			originalIntermediate = originalFinal - 1;
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			originalIntermediate = originalFinal + 1;
		}

		return originalIntermediate;
	}

	public static short AORS(short originalFinal, int op, String mutantID
	/* , boolean isForStmts */) {
		short originalIntermediate = originalFinal;
		if (MSGConstraints.U_PRE_INCREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			originalIntermediate = originalFinal;
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			originalIntermediate = (short) (originalFinal - 1);
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			originalIntermediate = (short) (originalFinal + 1);
		}

		return originalIntermediate;
	}

	public static byte AORSValue(byte original, int op) {

		byte returnValue = original;

		if (MSGConstraints.U_PRE_INCREMENT == op) {
			returnValue = (byte) (original + 1);
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			returnValue = (byte) (original - 1);
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			returnValue = (byte) (original + 1);
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			returnValue = (byte) (original - 1);
		}

		return returnValue;
	}

	public static char AORSValue(char original, int op) {

		char returnValue = original;

		if (MSGConstraints.U_PRE_INCREMENT == op) {
			returnValue = (char) (original + 1);
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			returnValue = (char) (original - 1);
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			returnValue = (char) (original + 1);
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			returnValue = (char) (original - 1);
		}

		return returnValue;
	}

	public static double AORSValue(double original, int op) {

		double returnValue = original;

		if (MSGConstraints.U_PRE_INCREMENT == op) {
			returnValue = original + 1;
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			returnValue = original - 1;
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			returnValue = original + 1;
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			returnValue = original - 1;
		}

		return returnValue;
	}

	public static float AORSValue(float original, int op) {

		float returnValue = original;

		if (MSGConstraints.U_PRE_INCREMENT == op) {
			returnValue = original + 1;
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			returnValue = original - 1;
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			returnValue = original + 1;
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			returnValue = original - 1;
		}

		return returnValue;
	}

	public static int AORSValue(int original, int op) {

		int returnValue = original;
		if (MSGConstraints.U_PRE_INCREMENT == op) {
			returnValue = original + 1;
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			returnValue = original - 1;
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			returnValue = original + 1;
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			returnValue = original - 1;
		}

		return returnValue;
	}

	public static long AORSValue(long original, int op) {

		long returnValue = original;

		if (MSGConstraints.U_PRE_INCREMENT == op) {
			returnValue = original + 1;
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			returnValue = original - 1;
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			returnValue = original + 1;
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			returnValue = original - 1;
		}

		return returnValue;
	}

	public static short AORSValue(short original, int op) {

		short returnValue = original;

		if (MSGConstraints.U_PRE_INCREMENT == op) {
			returnValue = (short) (original + 1);
		} else if (MSGConstraints.U_PRE_DECREMENT == op) {
			returnValue = (short) (original - 1);
		} else if (MSGConstraints.U_POST_INCREMENT == op) {
			returnValue = (short) (original + 1);
		} else if (MSGConstraints.U_POST_DECREMENT == op) {
			returnValue = (short) (original - 1);
		}

		return returnValue;
	}
}
